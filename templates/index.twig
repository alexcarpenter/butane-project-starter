{% extends '_layout' %}

{% set title = 'Home' %}

{% block _inline_css %}
  <style>
    {{ source ('_inlineAssets/index_critical.min.css', ignore_missing = true) }}
  </style>
{% endblock %}

{% block body %}
  {{ macro.icon('icon', 'icon-facebook') }}

  {# {% for entry in craft.entries.section('blog') %}
    {% include '_includes/blocks' with { handle: entry.blocks } %}
  {% endfor %} #}
{% endblock %}

{# {% includeJsFile "/assets/js/index.js" %} #}
